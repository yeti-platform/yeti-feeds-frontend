import type { ObjectTypeDefinition } from "@/definitions/types";

// Drop your own indicator type definitions in here; they get merged into
// INDICATOR_TYPES. See ./README.md.
export const PRIVATE_INDICATOR_TYPES: ObjectTypeDefinition[] = [];
