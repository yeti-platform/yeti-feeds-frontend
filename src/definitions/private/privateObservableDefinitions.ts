import type { ObjectTypeDefinition } from "@/definitions/types";

// Drop your own observable type definitions in here; they get merged into
// OBSERVABLE_TYPES. See ./README.md.
export const PRIVATE_OBSERVABLE_TYPES: ObjectTypeDefinition[] = [];
