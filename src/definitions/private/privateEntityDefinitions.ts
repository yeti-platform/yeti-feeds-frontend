import type { ObjectTypeDefinition } from "@/definitions/types";

// Drop your own entity type definitions in here; they get merged into
// ENTITY_TYPES. See ./README.md.
export const PRIVATE_ENTITY_TYPES: ObjectTypeDefinition[] = [];
